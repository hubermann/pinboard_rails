#pins.transitions-enabled
  - @pins.each do |pin|
    .box.panel.panel-default
      - if pin.image.present?
        = link_to (image_tag pin.image.url() ) 
      .panel-body
        %h2= link_to pin.title, pin
        %p.user
          Submitted by:
          = pin.user.email